name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore
        run: dotnet restore SeanShell.sln
      - name: Build
        run: dotnet build SeanShell.sln -c Release --no-restore
      - name: Test
        run: dotnet test SeanShell.sln -c Release --no-build
