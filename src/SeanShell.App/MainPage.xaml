<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SeanShell.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SeanShell.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        This is the main content area of your application.
        Add your UI elements here — for example, controls, layouts, and data-bound views.
        Use 'dotnet new winui-page' to create additional pages and navigate to them via
        the Frame in MainWindow (RootFrame).
    -->
    <ScrollViewer>
        <Grid Padding="32" RowSpacing="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Developer workspace" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="A safe companion shell running beside Windows Explorer."
                           TextWrapping="Wrap" />
            </StackPanel>
            <Grid Grid.Row="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Launcher" />
                        <TextBlock Text="Search installed apps and safe system commands." TextWrapping="Wrap" />
                        <Button
                            HorizontalAlignment="Left"
                            Click="OnOpenLauncherClicked"
                            Content="Open Launcher" />
                        <TextBlock
                            x:Name="ShortcutStatus"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Keyboard shortcut: Alt + Space"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Repositories" />
                        <TextBlock Text="Git status providers arrive through plugins." TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Shell state" />
                        <TextBlock x:Name="ModeText" Text="Normal" />
                    </StackPanel>
                </Border>
            </Grid>
            <StackPanel Grid.Row="2" Spacing="12">
                <ToggleSwitch x:Name="GamingModeToggle"
                              Header="Gaming mode"
                              OffContent="Background providers active"
                              OnContent="Optional providers paused"
                              Toggled="OnGamingModeToggled" />
                <InfoBar IsOpen="True"
                         IsClosable="False"
                         Message="Explorer remains active; this preview does not hook or inject into games."
                         Severity="Informational"
                         Title="Compatibility-first" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
