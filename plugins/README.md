# Plugins

Built-in plugins live here behind host lifecycle timeouts and fault isolation.
The Developer tools, Git repository, WSL distribution, Docker container, and .NET
workspace plugins are implemented. The .NET provider focuses on C#, ASP.NET Core,
and Blazor projects.

See [`docs/plugin-spec.md`](../docs/plugin-spec.md) for the draft contract and
security boundary.
